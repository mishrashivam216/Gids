
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="CustomProgressBar" parent="Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:progressDrawable">@drawable/custom_progress_drawable</item>
        <item name="android:indeterminateDrawable">@drawable/custom_progress_drawable</item>
        <item name="android:indeterminateTint">@color/colorAccent</item> <!-- Customize color for indeterminate state -->
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>



</resources>